import styled from "styled-components"

export const Description = styled.p`
    font-size: 16px;
    font-weight: 400;
    line-height: 28px;
    color: var(--text-secondary);
    text-align: justify;
`;